This example records from the microphone, using either `rec` or `sox` command line utilities,
and sends the audio to DeepSpeech.  Output's the speech recognition results to the console.

```
npm install
node start.js
```